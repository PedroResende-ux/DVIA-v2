I decided to take a step back from trying to decrypt and patch a large app so I can learn the workflow on something simpler instead of having to rely on other sources to explain me everything.

First, I looked for a repository where I could practice the full process end to end.

DVIA is already decrypted, so I do not need to use tools like Frida for this part.

The goal of this project is to learn static patching, which means modifying the app files on disk.

I opened the workspace of the project on xcode

